# Production Environment Configuration for Amplify
# Copy this to .env.production and fill in your values

# ============================================
# Backend Configuration (Choose one option)
# ============================================

# Option A: EC2 Backend (with local models)
# Replace <EC2_IP_OR_DOMAIN> with your EC2 public IP or domain
# Use wss:// for HTTPS (requires SSL certificate on backend)
# Use ws:// for HTTP (development only, not secure)
VITE_BACKEND_WS=wss://<EC2_IP_OR_DOMAIN>:8000/ws

# Option B: No backend (use Remote APIs only)
# Comment out VITE_BACKEND_WS and use API keys below

# ============================================
# Remote API Keys (Optional)
# ============================================
# Uncomment and fill if using OpenAI or Grok APIs
# VITE_OPENAI_API_KEY=sk-your-openai-api-key-here
# VITE_XAI_API_KEY=xai-your-grok-api-key-here

# ============================================
# Notes
# ============================================
# 1. For Amplify deployment, set these as Environment Variables in Amplify Console
# 2. DO NOT commit real API keys to Git
# 3. For SSL/WSS, configure Let's Encrypt on your EC2 backend (see AMPLIFY_SETUP.md)
# 4. Example backend URLs:
#    - EC2 with domain: wss://api.yourdomain.com/ws
#    - EC2 with IP: ws://1.2.3.4:8000/ws (HTTP only, not recommended)
#    - ALB: wss://your-alb-12345.us-east-1.elb.amazonaws.com/ws
